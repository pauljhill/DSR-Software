/**
 * Global configuration settings
 */

const config = {
  // API endpoints
  api: {
    base: '/api',
    shows: '/api/shows',
    equipment: '/api/equipment',
    pdf: '/api/pdf'
  },
  
  // File paths
  paths: {
    data: '/data',
    shows: '/data/shows',
    templates: '/data/templates'
  }
};

export default config;